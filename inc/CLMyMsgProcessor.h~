#ifndef CL_MSG_PROCESSOR_H
#define CL_MSG_PROCESSOR_H

#include "CLStatus.h"
#include "CLMessage.h"
#include "CLMessageObserver.h"

class CLMyMsgProcessor : public CLMessageObserver {
 public:
  CLMyMsgProcessor();
  ~CLMyMsgProcessor();

  CLStatus Initialize(void * pContext);
  CLStatus DispatchMessage(CLMessage * pMessage);
};

#endif
